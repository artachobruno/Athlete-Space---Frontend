services:
  - type: static
    name: athlete-space-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        sync: false
        # Set this to your backend API URL in Render dashboard
        # Example: https://your-backend.onrender.com

